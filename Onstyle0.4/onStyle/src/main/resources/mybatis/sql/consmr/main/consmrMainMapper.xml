<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >	

<mapper namespace="consmrMainMapper">
	    
	<select id="mainProdctSelect" resultType="prodctVo">
		SELECT prodct_seq, prodct_img_route1
		FROM prodct
<!-- 		WHERE prodct_disply_state = 'M' -->
    </select>
</mapper>